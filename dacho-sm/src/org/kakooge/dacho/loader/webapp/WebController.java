package org.kakooge.dacho.loader.webapp;


public class WebController {

	public void start() {
		/*
		 * // at startup Map args = new HashMap(); args.put("webroot",
		 * "<my webroot dir>"); // or any other command line args, eg port
		 * Launcher.initLogger(args); Launcher winstone = new Launcher(args); //
		 * spawns threads, so your application doesn't block
		 * 
		 * ... (your application code)
		 * 
		 * // before shutdown winstone.shutdown();
		 */
	}

	public void stop() {

	}
}
